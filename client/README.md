# UAM Transcription UI

```bash
python -m venv venv && source venv/bin/activate
```

```bash
pip install -r requirements.txt
```

```bash
uvicorn web_interface:app --host 0.0.0.0 --port 8001 --reload
```
